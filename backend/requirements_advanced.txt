# Advanced UI Replication System - Requirements
# This adds visual style extraction and text detection for pixel-perfect code generation

# Core dependencies (already installed)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
pydantic>=2.0.0
tensorflow>=2.13.0

# NEW: OCR and text detection
pytesseract>=0.3.10
easyocr>=1.7.0

# Optional: Better text detection
# paddleocr>=2.7.0  # Uncomment for better Asian language support
